{
  "name": "claude-session-cron",
  "version": "1.0.0",
  "description": "Telegram bot that pings your local Claude Code CLI on a schedule (Tehran time) to keep your usage session/quota window active.",
  "main": "src/bot.js",
  "scripts": {
    "start": "node src/bot.js"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-cron": "^3.0.3",
    "node-telegram-bot-api": "^0.66.0"
  }
}
